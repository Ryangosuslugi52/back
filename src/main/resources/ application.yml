datasources:
  default:
    url: //
    driverClassName: //
    username: //
    password: //
    dialect: //

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update

logger:
  levels:
    org.hibernate: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE